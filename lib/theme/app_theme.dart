import 'package:flutter/material.dart';

class AppColors {
  static const blue = Color(0xFF4EB6F7);
  static const bg = Color(0xFF0B0F14);          // near-black
  static const surface = Color(0xFF121822);     // dark slate
  static const surface2 = Color(0xFF0F141C);    // alt surface
  static const border = Color(0xFF253042);      // subtle outline
  static const text = Color(0xFFE8EEF6);
  static const textMuted = Color(0xFF9FB0C3);
}

class AppRadii {
  static const r12 = BorderRadius.all(Radius.circular(12));
  static const r16 = BorderRadius.all(Radius.circular(16));
  static const r20 = BorderRadius.all(Radius.circular(20));
}

class AppSpacing {
  static const s8 = 8.0;
  static const s12 = 12.0;
  static const s16 = 16.0;
  static const s20 = 20.0;
}
